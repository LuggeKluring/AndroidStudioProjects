<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.williamanderssonber.randomgrejer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_account_circle_white_36dp"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_account_circle_white_36dp"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Encryption" android:parentActivityName=".MainActivity">
        </activity>
    </application>

</manifest>